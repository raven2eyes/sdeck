# Bedroom Stream Deck +
extends: defaults
serial: CHANGE_ME_BEDROOM

templates:
  touchscreen:
    - slot: 1
      template: LightCard.dui
      controller: light
      config:
        entity: bedroom_lights
